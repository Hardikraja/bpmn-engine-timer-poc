// Utility Functions for State Management and Execution Printing

// Function to manage state transitions
export function transitionState(currentState, newState) {
    // Logic for transitioning to a new state
    return newState;
}

// Function to print execution details
export function printExecutionDetails(details) {
    console.log(`Execution Details: ${JSON.stringify(details, null, 2)}`);
}